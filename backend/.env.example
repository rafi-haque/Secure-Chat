# Environment Configuration for Secure Chat Backend
# Copy this file to .env and update the values for your setup

# Server Configuration
NODE_ENV=development
PORT=5000
FRONTEND_URL=http://localhost:3000

# Database Configuration
# Replace with your MongoDB connection string
MONGODB_URI=mongodb://localhost:27017/secure-chat-dev

# Security Configuration
# IMPORTANT: Generate a strong, unique JWT secret for production
JWT_SECRET=your-super-secret-jwt-key-change-in-production-use-openssl-rand-hex-64
BCRYPT_ROUNDS=12

# WebSocket Configuration (deprecated - WebSocket now runs on same port as HTTP)
WEBSOCKET_PORT=3001

# Logging
LOG_LEVEL=info

# Development/Production flags
ENABLE_CORS=true
ENABLE_LOGGING=true
ENABLE_SECURITY_HEADERS=true

# API Rate Limiting (15 minutes = 900000ms)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Production Environment Variables
# Set these when deploying to production:
# - Use a proper database URL (MongoDB Atlas, etc.)
# - Generate secure JWT secret: openssl rand -hex 64
# - Set NODE_ENV=production
# - Update FRONTEND_URL to your domain